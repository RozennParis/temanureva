{% extends 'base.html.twig' %}

{% block title %}Article - NAO{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12">
                <div>
                    {{ article.content|raw }}
                </div>
                <p class="center">{{ article.user.lastname }} {{ article.user.firstname }} - {{ article.publishingDate|date("d/m/Y") }}</p>
            </div>
        </div>
    </div>
{% endblock %}