twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'materialize_layout.html.twig'
    globals:
        image_article_directory: '%image_article_directory%'
